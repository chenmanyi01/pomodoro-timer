# 番茄钟桌面应用 — 产品需求文档 (PRD)

> 版本：v1.0
> 状态：草稿
> 角色：资深全栈工程师 + UI 设计师

---

## 1. 产品概述

### 1.1 一句话描述
一个轻量、美观、开箱即用的桌面番茄钟，帮助用户以「25 分钟专注 + 5 分钟休息」的节奏高效工作。

### 1.2 核心价值
- **极简启动**：下载即用，零配置，零广告
- **不干扰**：专注时只显示计时器，不推送无关信息
- **数据在本地**：所有记录存在用户电脑上，无隐私顾虑

### 1.3 目标用户
- 程序员、设计师、写作者等需要长时间专注的知识工作者
- 准备考研/考公/考试的学生群体
- 注意力容易分散、想尝试番茄工作法的入门用户

---

## 2. 功能需求

### 2.1 核心计时器（P0）

| 功能 | 说明 |
|------|------|
| 番茄计时 | 默认 25 分钟专注倒计时 |
| 短休息 | 默认 5 分钟 |
| 长休息 | 默认 15 分钟（每 4 个番茄后）|
| 开始/暂停 | 空格键切换，按钮切换 |
| 重置 | 重置当前阶段计时 |
| 跳过阶段 | 跳过当前工作/休息，进入下一阶段 |
| 自动流转 | 工作结束 → 自动进入休息（可配置）|
| 可视化倒计时 | 圆形进度环 + 数字时间显示 |

### 2.2 待办任务（P1）

| 功能 | 说明 |
|------|------|
| 添加任务 | 输入框 + Enter 键添加 |
| 完成任务 | 点击圆形 checkbox |
| 删除任务 | 悬停显示删除按钮 |
| 清除已完成 | 一键清除所有勾选任务 |
| 持久化 | localStorage 保存，关闭应用不丢失 |

### 2.3 统计数据（P1）

| 功能 | 说明 |
|------|------|
| 今日番茄数 | 当天完成的番茄个数 |
| 本周番茄数 | 近 7 天累计 |
| 总计番茄数 | 历史总累计 |
| 今日专注时长 | 今日总专注分钟数 |
| 近 7 天趋势图 | 柱状图展示每日专注时长 |
| 番茄点 | 8 个圆点直观显示今日进度 |

### 2.4 音效与通知（P1）

| 功能 | 说明 |
|------|------|
| 结束提醒音 | 每个阶段结束时播放 3 声提示音 |
| 桌面通知 | 系统原生通知，点击聚焦窗口 |
| 白噪音 | 可开关的低通滤波白噪音，帮助进入心流 |
| 白噪音音量 | 滑块调节 |

### 2.5 个性化设置（P2）

| 功能 | 说明 |
|------|------|
| 工作时长 | 可调（默认 25 min）|
| 短休息时长 | 可调（默认 5 min）|
| 长休息时长 | 可调（默认 15 min）|
| 长休息间隔 | 可调（默认 4 个番茄）|
| 暗色/亮色主题 | 一键切换 |
| 自动开始休息 | 工作结束自动开始休息 |
| 自动开始工作 | 休息结束自动开始工作 |

### 2.6 系统托盘（P2）

| 功能 | 说明 |
|------|------|
| 托盘图标 | 系统托盘显示应用图标 |
| 托盘提示 | 显示当前阶段和剩余时间 |
| 托盘菜单 | 显示窗口 / 退出 |
| 最小化到托盘 | 关闭按钮隐藏到托盘而非退出 |

---

## 3. UI/UX 设计规范

### 3.1 设计原则
- **内容优先**：计时器是绝对主角，占据视觉中心
- **克制**：不堆砌元素，每个 UI 都有其目的
- **愉悦感**：流畅动画、细腻圆角、阶段切换的色彩反馈

### 3.2 色彩系统

| Token | 暗色模式 | 亮色模式 | 用途 |
|-------|---------|---------|------|
| `--bg` | `#0f0f1a` | `#f0f2f5` | 主背景 |
| `--bg-card` | `#1a1a2e` | `#ffffff` | 卡片/容器 |
| `--text` | `#e8e8f0` | `#2c3e50` | 主文字 |
| `--text-secondary` | `#8888aa` | `#7f8c8d` | 次要文字 |
| `--primary` | `#E74C3C` | `#E74C3C` | 番茄红（工作）|
| `--break` | `#2ECC71` | `#2ECC71` | 绿色（短休息）|
| `--long-break` | `#3498DB` | `#3498DB` | 蓝色（长休息）|

### 3.3 布局结构

```
┌────────────────────────────────┐
│  🍅 番茄钟          🌙   ─  ✕ │  ← 自定义标题栏 (38px)
├────────────────────────────────┤
│                                │
│         ┌──────────────┐       │
│         │   25:00      │       │  ← SVG 圆形进度环
│         │   工作中      │       │     + 时间/状态显示
│         └──────────────┘       │
│                                │
│     ▶ 开始   ⟳ 重置   ⏭ 跳过   │  ← 控制按钮行
│                                │
│     ● ● ● ● ○ ○ ○ ○           │  ← 今日番茄进度点
│        今日完成: 0 个番茄       │
├────────────────────────────────┤
│  📋 任务  │  📊 统计  │  ⚙️ 设置 │  ← 标签栏
├────────────────────────────────┤
│  [+ 添加任务...]               │
│  ○ 写需求文档                  │  ← 任务列表
│  ○ 设计 UI 原型               │
│  ● 安装开发环境                │
│  2/3 已完成   清除已完成       │
└────────────────────────────────┘
```

### 3.4 交互细节
- **环形动画**：计时进行时，SVG 圆环平滑填充；暂停时暂停动画
- **阶段色彩切换**：工作=红色，短休息=绿色，长休息=蓝色，全局主题色联动
- **脉冲动效**：运行时数字区域有呼吸式缩放动画
- **任务动效**：新增任务滑入，删除淡出
- **无边框窗口**：自定义标题栏，支持拖拽

### 3.5 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Space` | 开始/暂停 |
| `R` | 重置 |
| `S` | 跳过阶段 |
| `N` | 聚焦任务输入框 |

---

## 4. 技术架构

### 4.1 技术选型

| 层级 | 技术 | 选型理由 |
|------|------|---------|
| 壳层 | **Electron** | 成熟的桌面应用框架，跨平台 |
| UI | **原生 HTML + CSS + JS** | 零依赖，包体小，加载快 |
| 持久化 | **localStorage** | 数据量小（KB 级别），无需数据库 |
| 音效 | **Web Audio API** | 无需音频文件，纯代码合成 |
| 图表 | **Canvas 2D** | 轻量，7 天柱状图足够 |
| 打包 | **electron-builder** | 输出 Windows exe 安装包/便携版 |

### 4.2 项目结构

```
番茄钟/
├── package.json          # 项目配置 + electron-builder 打包配置
├── main.js               # Electron 主进程：窗口/托盘/通知
├── preload.js            # 预加载脚本：IPC 桥接
├── renderer/
│   ├── index.html        # UI 结构
│   ├── styles.css        # 全部样式（暗色+亮色主题）
│   └── app.js            # 应用逻辑
└── dist/                 # 打包输出（gitignore）
```

### 4.3 核心模块

| 模块 | 职责 | 关键 API |
|------|------|---------|
| `Timer` | 倒计时引擎 | `start()` `pause()` `reset()` `onComplete` |
| `TaskManager` | 任务 CRUD + 持久化 | `add()` `toggle()` `remove()` `clearCompleted()` |
| `StatsManager` | 统计数据 + 持久化 | `recordPomodoro()` `getLast7Days()` |
| `SoundEngine` | 提示音 + 白噪音 | `playAlarm()` `startWhiteNoise()` `stopWhiteNoise()` |
| `PomodoroApp` | 主控制器，串联所有模块 | 事件绑定、快捷键、UI 更新 |

### 4.4 数据模型

```js
// 设置
{
  workDuration: 25,      // 分钟
  shortBreak: 5,
  longBreak: 15,
  longInterval: 4,
  notification: true,
  sound: true,
  autoBreak: true,
  autoWork: false,
  whiteNoise: false,
  noiseVolume: 30,
  darkMode: true
}

// 任务
{ id: "timestamp", text: "写需求文档", done: false, createdAt: 1712345678901 }

// 统计
{ days: { "2026-05-08": { count: 4, focusMinutes: 100 } } }
```

### 4.5 安全与边界
- 无网络请求，不收集任何数据
- 所有数据存储在用户电脑本地
- 无外部依赖（除 Electron 壳层）
- 窗口最小尺寸：380×600

---

## 5. 开发阶段

### Phase 1 — 核心 MVP（当前）
- 完整计时器功能（工作/短休息/长休息）
- 开始/暂停/重置/跳过
- 圆形进度动画
- 阶段自动流转
- 桌面通知 + 提示音
- 暗色/亮色主题
- 任务列表
- 统计面板 + 图表

### Phase 2 — 增强（后续规划）
- 白噪音
- 系统托盘
- 配置自定义时长
- 快捷键

### Phase 3 — 扩展（远期规划）
- 番茄钟专注排行榜（可选联网）
- 导出统计数据 (CSV/JSON)
- 自定义主题色
- 整点报时

---

## 6. 交付物

| 产出 | 说明 |
|------|------|
| 源码 | 完整项目代码，含注释 |
| 安装包 | Windows exe（可安装/便携版二选一）|
| 使用说明 | 简洁的 README 或内置引导 |

---

*文档结束*
